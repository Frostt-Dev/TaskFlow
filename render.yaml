services:
  - type: web
    name: taskflow-api
    env: node
    plan: free
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: PORT
        value: 5000
      - key: MONGODB_URI
        sync: false
        placeholder: "Enter your MongoDB Atlas Connection String"
